;;;; -*- mode: lisp; syntax: common-lisp; base: 10 -*-
;;;; main.lisp

(uiop:define-package #:nur/src/main
  (:use #:cl))

(in-package #:nur/src/main)

(defun main (argv)
  (declare (ignore argv))
  (success))
